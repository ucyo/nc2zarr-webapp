import { TruncatePipePipe } from './truncate.pipe';

describe('TruncatePipePipe', () => {
  it('create an instance', () => {
    const pipe = new TruncatePipePipe();
    expect(pipe).toBeTruthy();
  });
});
